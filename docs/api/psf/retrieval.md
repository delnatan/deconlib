# Phase Retrieval

Algorithms for recovering pupil phase from PSF measurements.

!!! note "Not re-exported from `deconlib.psf`"
    These live in `deconlib.psf.pupil_retrieval.retrieval`, which isn't
    currently re-exported at the top of the `psf` package — import from the
    full path shown below, not `deconlib.psf.retrieve_phase`.

## Functions

::: deconlib.psf.pupil_retrieval.retrieval.retrieve_phase

::: deconlib.psf.pupil_retrieval.retrieval.retrieve_phase_vectorial

## Result Class

::: deconlib.psf.pupil_retrieval.retrieval.PhaseRetrievalResult
    options:
      show_source: false
